<Window x:Class="FileDiff.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileDiff"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded"
				TextOptions.TextFormattingMode ="Display">

	<Window.Resources>
		<DataTemplate x:Key="ListTemplate">
			<StackPanel Orientation= "Horizontal">
				<TextBlock Text="{Binding LineNumber}" Width="40"/>
				<TextBlock Text="{Binding Text}" HorizontalAlignment="Center"/>
			</StackPanel>
		</DataTemplate>
	</Window.Resources>

	<DockPanel>

		<!--Menu-->
		<Border DockPanel.Dock="Top" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource  {x:Static SystemColors.ScrollBarBrushKey} }">
			<Grid x:Name="GridMenu">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition />
				</Grid.ColumnDefinitions>
				<Menu Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" VerticalAlignment="Center" Margin="2">
					<MenuItem Header="File">
						<MenuItem Header="Exit" />
					</MenuItem>
					<MenuItem Header="Tools">
						<MenuItem Header="Options" />
					</MenuItem>
					<MenuItem Header="Help">
						<MenuItem Header="About" />
					</MenuItem>
				</Menu>
			</Grid>
		</Border>

		<!--Main Toolbar-->
		<WrapPanel DockPanel.Dock="Top" x:Name="ToolbarMain" Orientation="Horizontal">
			<WrapPanel.Background>
				<LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
					<GradientStop Color="{DynamicResource {x:Static SystemColors.WindowColorKey}}" Offset="0" />
					<GradientStop Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}" Offset="1" />
				</LinearGradientBrush>
			</WrapPanel.Background>
			<Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Focusable="False" Command="ApplicationCommands.New">
				<StackPanel Orientation="Horizontal">
					<Image  VerticalAlignment="Center" Margin="3" />
					<TextBlock Text="A" VerticalAlignment="Center" />
				</StackPanel>
			</Button>
			<Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Focusable="False" Command="ApplicationCommands.Save">
				<StackPanel Orientation="Horizontal">
					<Image  VerticalAlignment="Center" Margin="3" />
					<TextBlock Text="B" VerticalAlignment="Center" />
				</StackPanel>
			</Button>
			<Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey }}" Focusable="False" Command="ApplicationCommands.Delete" >
				<StackPanel Orientation="Horizontal">
					<Image  VerticalAlignment="Center" Margin="3" />
					<TextBlock Text="C" VerticalAlignment="Center" />
				</StackPanel>
			</Button>
		</WrapPanel>

		<!--Status Bar-->
		<Grid x:Name="GridStatusbar" DockPanel.Dock="Bottom"  Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
			<TextBlock Text="Status" Margin="2"/>
		</Grid>

		<Grid >

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<Border Grid.Column="0" Background="AliceBlue" BorderThickness="0,1,1,1" BorderBrush="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey} }">
				<StackPanel Grid.Column="0">
					<TextBox Text="{Binding LeftFile}" />
					<ItemsControl ItemsSource="{Binding LeftSide}" ItemTemplate="{DynamicResource ListTemplate}"/>
				</StackPanel>
			</Border>

			<GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" Focusable="False"/>

			<Border Grid.Column="2" Background="#FFFFF8EA" BorderThickness="1,1,0,1" BorderBrush="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey} }">
				<StackPanel Grid.Column="2">
					<TextBox Text="{Binding RightFile}" />
					<ItemsControl ItemsSource="{Binding RightSide}" ItemTemplate="{DynamicResource ListTemplate}"/>
				</StackPanel>
			</Border>

		</Grid>



	</DockPanel>
</Window>
