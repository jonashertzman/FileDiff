<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:FileDiff" xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="FileDiff.BrowseFolderWindow" mc:Ignorable="d" Title="BrowseFolderWindow" Height="350" Width="450" TextOptions.TextFormattingMode="Display" UseLayoutRounding="True" ResizeMode="CanResizeWithGrip" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">

	<Grid Grid.Row="3" Margin="5">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<StackPanel Grid.Row="0" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="5">
			<ContentPresenter ContentTemplate="{StaticResource DriveIcon}" Style="{StaticResource CommonButtonIcon}" />
			<ContentPresenter ContentTemplate="{StaticResource FolderIcon}" Style="{StaticResource CommonButtonIcon}" />
		</StackPanel>

		<Label Grid.Row="1" Content="Path" Style="{StaticResource CommonTextLabel}" />
		<TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Style="{StaticResource CommonTextBox}" />

		<TreeView x:Name="FolderTree" Grid.Row="2" Grid.ColumnSpan="4" Margin="5" BorderThickness="1" BorderBrush="{StaticResource  {x:Static SystemColors.ScrollBarBrushKey} }" TreeViewItem.Expanded="TreeViewItem_Expanded">
			<TreeView.Resources>
				<Style TargetType="{x:Type TreeViewItem}">
					<!-- Style for the selected item -->
					<Setter Property="BorderThickness" Value="1" />
					<Setter Property="HeaderTemplate">
						<Setter.Value>
							<DataTemplate>
								<StackPanel Orientation="Horizontal" Margin="0">
									<ContentPresenter ContentTemplate="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TreeViewItem}}, Path=Header, Converter={x:Static local:HeaderToImageConverter.Instance}}" Style="{StaticResource CommonButtonIcon}" />
									<TextBlock Text="{Binding}" Margin="5,0" VerticalAlignment="Center" />
								</StackPanel>
							</DataTemplate>
						</Setter.Value>
					</Setter>
					<Style.Triggers>
						<!-- Selected and has focus -->
						<Trigger Property="IsSelected" Value="True">
							<Setter Property="BorderBrush" Value="#99D1FF" />
						</Trigger>

						<!-- Selected but does not have the focus -->
						<MultiTrigger>
							<MultiTrigger.Conditions>
								<Condition Property="IsSelected" Value="True" />
								<Condition Property="IsSelectionActive" Value="False" />
							</MultiTrigger.Conditions>
							<Setter Property="BorderBrush" Value="#D9D9D9" />
						</MultiTrigger>
					</Style.Triggers>

					<Style.Resources>
						<Style TargetType="Border">
							<Setter Property="CornerRadius" Value="2" />
						</Style>
					</Style.Resources>
				</Style>

				<!-- Brushes for the selected item -->
				<SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FFCBE8F6" />
				<SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black" />
				<SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="Black" />
			</TreeView.Resources>
		</TreeView>

		<Button Grid.Row="3" Grid.Column="2" Content="OK" Style="{StaticResource CommonButton}" />
		<Button Grid.Row="3" Grid.Column="3" Content="Cancel" Style="{StaticResource CommonButton}" />
	</Grid>

</Window>
